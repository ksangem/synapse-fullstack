# Dependencies
node_modules/

# Environment (contains secrets)
.env
packages/backend/.env
packages/frontend/.env
packages/frontend/.env.local

# Build output
dist/
build/
.next/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Excel temp/lock files
~$*

# Personal work log (kept local, never pushed). Paths follow the 2026-07-27 move into
# planning/ — these were root-anchored names, so leaving them would have started
# committing the personal log.
planning/work-log.xlsx
planning/generators/build-worklog.py

# Local verification screenshots
verify-*.png

# Logs
*.log
npm-debug.log*

# Debug/scratch probe output (leading-underscore files at repo/package root only;
# scoped so it never shadows tracked files like src/db/migrations/meta/_journal.json)
/_*.json
/_*.txt
/packages/backend/_*.json
/packages/backend/_*.txt

# Docker volumes
synapse_pgdata/

# Claude internal
.claude/
CLAUDE.md
CLAUDE_v2_1.md
